# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pkl

# Virtual environments
.venv/
venv/
env/

# Unit test / coverage
htmlcov/
.tox/
.coverage
.coverage.*
.cache/
nosetests.xml
coverage.xml
*.cover
*.py,cover
.pytest_cache/

# mypy / type-checking
.mypy_cache/
.dmypy.json
dmypy.json

# --- Editor / OS junk -------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride

# VS Code
.vscode/
.history/

# JetBrains
.idea/

# --- Node / frontend (if ever added) ---------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Environment / secrets --------------------------------------------------
.env
.env.*
!.env.example

# --- Docker / Compose -------------------------------------------------------
# Docker build artifacts
*.log
dump.rdb

# Named/anonymous volumes are managed by Docker, not Git
# (kept here for clarity even though they are not under the repo tree)
# ollama-data
# chat-doc-store

# --- Application data -------------------------------------------------------
# In-container doc store is under /app/doc_store (a volume),
# but avoid accidentally committing any local runs if you mount it to host.
doc_store/
*.jsonl

# --- Misc -------------------------------------------------------------------
*.swp
*.swo
